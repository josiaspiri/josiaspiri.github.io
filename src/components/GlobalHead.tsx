import React from 'react';

export const Head = () => (
  <>
    <title>Hello, World!</title>
    <meta
      name="description"
      content="I don't know where I'm going but I'll know when I get there."
    />
    <body className="min-h-screen min-w-screen bg-black flex flex-col justify-center" />
  </>
);

export default Head;
